include "ElectroWeakAnalysis/WReco/data/l1RPC_EventContent.cff"

    module L1RPCEventContent = PoolOutputModule
    {
        untracked string fileName = "l1RPC.root"
        using l1RPC_EventContent
        using l1RPCEventSelection
        untracked PSet datasets ={
    		untracked string dataTier = "USER"
   		untracked string filterName = "l1RPC_Filter"
        }
    }

