process RPCFakeCalib = {
        
  source = EmptyIOVSource {
      string timetype = "runnumber"
      untracked uint32 firstRun = 1
      untracked uint32 lastRun = 1
      uint32 interval = 1
  }

  source = EmptySource {}
  untracked PSet maxEvents = { untracked int32 input = 1 }

  include "CalibMuon/RPCCalibration/data/rpcFakeCalib.cfi"

  module get = EventSetupRecordDataGetter { 
                VPSet toGet = {
                {string record = "RPCStripNoisesRcd" vstring data = {"RPCStripNoises"} }}
                untracked bool verbose = true 
  }

  module print = AsciiOutputModule {}

  path p = {get}
  endpath this_is_the_end = {print}

}
